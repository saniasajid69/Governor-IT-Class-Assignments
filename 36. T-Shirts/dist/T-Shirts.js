"use strict";
//T-Shirt: write a function called make_shirt() that accepts a size and the text of message that should be printed on the shirt.The function should print a sentence sumarizind the size f the shirt and the message ptinted on it.
//Cal the function.
function make_shirt(size, text) {
    console.log(`Creating a${size} shirt with message: ${text}`);
}
make_shirt('large', 'Hello world');
make_shirt('medium', 'Hello world');
make_shirt('small', 'Hello world');
